<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <!-- The media-session beacon runs as a specialUse FGS (not dataSync): Android 15
         bans starting a dataSync FGS from a BOOT_COMPLETED receiver, and we need the
         beacon to come up at boot so cast/background playback is detected without the
         user opening the app. specialUse has no such boot restriction. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- ConnectivityManager.registerNetworkCallback (NetworkMonitor): the reconnect
         loops re-dial the moment a network appears — notably the Tailscale tunnel
         the desktop is reached over — instead of sitting out an exponential backoff
         that may already be at its 5-minute ceiling. Normal permission, no prompt. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- mDNS/Zeroconf desktop discovery (NsdManager). -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <!-- Dictation: the phone is the desktop's microphone (DictationService). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <!-- Listen to desktop: desktop audio plays here (ListenService). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <!-- Headphone awareness: the Bluetooth headset's name and battery level. -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <!-- Desktop auth: the phone's fingerprint unlocks a Keystore signing key
         (DesktopAuthKey) whose signature the desktop accepts for sudo / polkit /
         unlock. BiometricPrompt needs this; the key itself is gated in hardware. -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lattice">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- QR device flow: the desktop pairing script (control panel › Pair a Phone) renders
                 lattice://pair?v=1&code=…&host=<magicdns>&lan=<ip> as a QR.
                 The stock camera app scans it and fires this VIEW intent — no
                 in-app scanner, no camera permission. MainActivity stores the
                 code, then verifies host candidates via the 4905 HMAC
                 challenge-response and keeps whichever really answers. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="lattice" android:host="pair" />
            </intent-filter>
        </activity>

        <!-- One desktop challenge at a time. Over the lock screen on purpose:
             the desktop asks exactly when the phone is on the nightstand. -->
        <activity
            android:name=".AuthPromptActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleTop"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:theme="@style/Theme.Lattice" />
        <receiver
            android:name=".AuthActionReceiver"
            android:exported="false" />

        <service
            android:name=".AudioCaptureService"
            android:exported="false"
            android:foregroundServiceType="mediaProjection" />

        <service
            android:name=".MediaSessionBeaconService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="media_session_beacon" />
        </service>

        <!-- Restart the beacon after a reboot so background/cast playback is detected
             without the user opening the app. Guarded inside the receiver: only starts
             when a host is configured AND Notification Access is granted. -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".DictationService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <service
            android:name=".ListenService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <!-- Quick Settings tile: the dictation hotkey, one swipe from anywhere. -->
        <service
            android:name=".DictationTile"
            android:exported="true"
            android:icon="@drawable/ic_stat_mic"
            android:label="Dictate"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <meta-data android:name="android.service.quicksettings.ACTIVE_TILE" android:value="false" />
        </service>

        <service
            android:name=".MediaSessionListener"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>
    </application>
</manifest>
